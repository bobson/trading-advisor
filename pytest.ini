[pytest]
# Put the repo root on sys.path so `import src...` works whether tests are run as
# `pytest` (CI) or `python -m pytest` (local). Fixes "ModuleNotFoundError: No module named 'src'".
pythonpath = .
markers =
    network: tests that make real network calls (excluded with -m "not network")
