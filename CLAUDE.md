# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

**`PLAN.md`** records the completed core build (Phases 0–10) and its folder layout. **`trading-advisor-plan.md`** is now the authoritative **forward roadmap** (Phases 11–26): one linear, de-duplicated, **validation-first** sequence — the backtest harness (Phase 14) is built early and is the instrument that validates every later analytical upgrade before it earns a vote. Build one phase per session; the working core is tagged `v1-core-advisor`; do each upgrade from Phase 12 onward on its own branch and merge only when that phase's "done when" passes. (The old `PLAN2.md` was merged into `trading-advisor-plan.md` and deleted.)

**Done:** Phase 0 (skeleton), Phase 1 (`src/data/`), Phase 2 (`src/indicators/features.py`), Phase 3 (`src/structure/swings.py`), Phase 4 (`support_resistance.py`, `trendlines.py`, `trend.py`), Phase 5 (`src/structure/fibonacci.py`, `scripts/show_fibonacci.py`), Phase 6 (`src/signals/confluence.py`, `scripts/show_confluence.py`), Phase 7 (`src/advisor/facts.py`, `src/advisor/explain.py`, `scripts/show_explanation.py`), Phase 8 (`src/viz/chart.py`, `scripts/analyze.py`), Phase 9 (`src/patterns/chart_patterns.py`, `scripts/show_patterns.py`, wired into `facts.py`), Phase 10 (`src/advisor/vision.py`, `scripts/analyze_image.py`). **ALL PHASES 0–10 COMPLETE.**

Forward roadmap (`trading-advisor-plan.md`) progress — **Phase 12 done** (`src/service/analyze.py`), **Phase 13 done** (`src/data/{base,crypto_ccxt,forex_api,registry,quality}.py`), **Phase 14 done** (`src/backtest/evaluate.py`, `scripts/backtest.py`, `tests/test_backtest.py`), **Phase 15 done** (volume: `features.py` + `confluence.py` + `facts.py`), **Phase 16 done** (multi-timeframe: `src/structure/mtf.py`, `tests/test_mtf.py`), **Phase 17 done** (category-aware confidence: `confluence.py` rewrite), **Phase 18 done** (TA toolkit as FACTS: `features.py` + `structure/divergence.py` + `structure/round_numbers.py`), **Phase 19 done** (market adaptation: `src/market/adaptation.py`, `tests/test_adaptation.py`), **Phase 20 done** (robustness: `advisor/verify.py`, `explain_structured`, `tests/test_snapshot.py`), **Phase 21 done** (context layer: `src/context/`, `tests/test_context.py`), **Phase 22 FACTS done / vote DEFERRED** (derivatives: `src/derivatives/`, `tests/test_derivatives.py`).

Phase 22 conventions (⚠️ PARTIAL — facts shipped, the "feed the score" vote is NOT done): crypto perp positioning as FACTS only, mirroring Phase 21. `src/derivatives/`: `funding.py` (`read_funding` — current perp funding rate, classified crowded_longs/crowded_shorts/neutral vs `derivatives.funding_extreme`=0.0005/8h; contrarian), `open_interest.py` (`read_open_interest` — CURRENT OI only; binance caps OI *history* to ~30 days so no OI trend/backtest), `gather.py::gather_derivatives(symbol, cfg, *, exchange=None)` (CRYPTO-ONLY — forex → None since no perp funding/OI; maps spot→perp via `_perp_symbol` BTC/USDT→BTC/USDT:USDT; NEVER raises; live source is `ccxt.binanceusdm()`). Same load-bearing rule as context: live-only, injected into `advise(..., derivatives=None)`, NEVER in `build_facts` (per-bar backtest → look-ahead); a test pins `advise(df=...)` no-derivatives → `facts["derivatives"] is None`. Rendered as a DERIVATIVES/POSITIONING block. **DEFERRED (do NOT claim done): (1) the funding forward-return study + a funding VOTE — the done-when's "feed the score" clause; funding history IS available ≥6mo (verified) so this is runnable later (forward-fill 8h funding onto bars, look-ahead-safe, compare post-extreme forward returns vs baseline), promote ONLY if it earns it. (2) long/short + liquidations (CoinGlass, paid). User chose "just show it" this session given the visual/UI goal.** Verified live: BTC perp funding + OI fetch cleanly; `EUR/USD` → None.

Phase 21 conventions: background facts (sentiment/calendar/news) for Layer 2, NEVER touching detectors. `src/context/`: `sentiment.py` (crypto Fear & Greed, alternative.me, NO key — the one source VERIFIED live: 69/Greed), `calendar.py` + `news.py` (Finnhub, key-gated via `FINNHUB_API_KEY` in .env — PLUMBED but UNVERIFIED against live Finnhub shape until a key exists; return [] without a key), `http.py` (`get_json` with a hard 5s timeout so context fails fast, never hangs the pipeline), `gather.py::gather_context(symbol, cfg, *, fetch=None)` (aggregates enabled sources, NEVER raises — each degrades to None/[] on any error — and stamps `as_of` + per-reading dates so stale context can't read as present-tense). THE LOAD-BEARING RULE: context is CURRENT-state, fetched ONLY in the live path (`scripts/analyze.py` calls `gather_context` and passes it to `advise(..., context=...)`); it is NEVER fetched inside `advise` and NEVER in `build_facts` (which runs per-bar in the backtest — current sentiment on historical bars = look-ahead + would break the snapshot). `advise(context=None)` default is pinned by a test (`advise(df=...)` with no context → `facts["context"] is None`) so a later edit can't silently start hitting the network in the offline suite. Each `fetch_*` takes an injectable `fetch` for offline tests; live Fear & Greed is `@pytest.mark.network`. Rendered in `facts_to_prompt` as a MARKET CONTEXT block (labeled "context, not prediction"). Done-when "explanation references it when relevant" is key-gated manual verification (like Phase 7); offline asserts the mechanical part (context in facts_text when present, absent when None). `requests` added to requirements.txt.

Phase 20 conventions: three robustness pieces, all off the vote path (backtest trivially unchanged — the SNAPSHOT is the regression guard now). (1) `advisor/verify.py` enforces "Layer 2 never contradicts Layer 1": the authoritative price set is built from `facts_to_prompt(facts)` ITSELF (same `extract_prices` extractor on facts text and explanation — "did it invent a price it wasn't shown"), flagging cited numbers in the price-magnitude band (0.5×–1.5× last_close, two-sided so RSI/ratios on low-priced FX are excluded too) that match no facts number within `tolerance_pct`(1). ADVISORY — returns `{ok, issues}`, never raises/gates (magnitude filtering false-flags mid-priced assets like SOL; that's why it informs, not blocks). Wired into `advise()` as a `verification` field (None when no explanation — the keyless path) + `to_payload`. (2) `tests/test_snapshot.py` + committed `tests/fixtures/{btc_1h_sample.csv (350 real bars), expected_facts.json}`: `build_facts` on the fixed slice must equal the snapshot (catches silent detector drift); the test docstring carries the regenerate command with the discipline "read the diff, confirm intended, THEN regenerate". 350 bars so 4h-MTF/fib/patterns are actually exercised (a 60-bar slice would snapshot a degenerate all-None dict). (3) `explain_structured(facts_text, cfg, client=None) -> {setup, why, invalidation}` via a FORCED tool call (`ANALYSIS_TOOL` + `tool_choice`), model-agnostic like `explain`, defensive extraction (missing field → clear RuntimeError) — ready to feed the Phase-24 API. Fixtures live in `tests/fixtures/` (committed, NOT gitignored — the `/data/` ignore is repo-root-anchored).

Phase 19 conventions: `market/adaptation.py`'s `market_context(symbol, featured_df, cfg) -> MarketContext` makes the read market-aware WITHOUT touching any vote (facts-only → the regression backtest still reproduces 51.3%). `asset_class_for(symbol)` (crypto/forex) drives: `volume_type` real vs TICK (forex tick-volume is flagged weaker and caveated in the prompt — never presented like real volume), `is_24_7`, `active_session` (forex only, `_forex_session(utc_hour)` — Tokyo/London/NY/overlap/Sydney), `weekend_gap` (forex only — a gap >2× median crossing Fri→Sun), and `significant_move_pct = ATR × market_adaptation.significant_move_atr_mult(1.5) / price` (volatility-relative "normal move"; scales between an 80k BTC and a 1.10 EUR/USD). WHY facts-first (advisor-vetted): the done-when ("significant move scales via ATR") is satisfied by a FACT; wiring ATR-relative thresholds into the VOTES is deferred because it's an UNVALIDATABLE vote-path change — its whole purpose is cross-market but crypto is the only market with data (and ATR-relative is volatility-adaptive, so it is NOT ≈ the fixed 0.5% and WOULD move crypto votes, needing a backtest). Wired into `facts.py` as a `market_adaptation` block + a `volume.type` tag + a MARKET caveat block. Forex is testable now via `advise(..., df=injected)` (live provider is Phase 26); the weekend-gap test uses a Fri→Sun index. TODO(phase-26): revisit ATR-relative proximity AND tick-volume vote-weighting together, backtesting crypto first.

Phase 18 conventions: added the curated toolkit as FACTS/context, deliberately promoting NO new votes this phase (the done-when says "each tool in the facts and *(if promoted)* a vote" — promotion is optional). `features.py` adds ATR, ADX, Stochastic(k/d), Bollinger(upper/mid/lower/%B), OBV columns (all in `INDICATOR_COLUMNS`, always present; OBV all-NaN without a volume column). pandas-ta returns None on frames too short for a length, so `_col(frame, prefix)` guards the NoneType-subscript shape (same bug class that hit MACD); prefix-matching avoids hardcoding param suffixes (ADX_14, BBL_20_2.0). New `structure/divergence.py` (`find_rsi_divergence` — bearish=higher price high + lower RSI high, bullish=mirror, on the last two confirmed swings) and `structure/round_numbers.py` (`nearest_round_number` — step = one order of magnitude below price, so 79381→79000, magnitude-adaptive; FX may want finer steps, revisit in Phase 19/26). Facts gained `volatility{atr,atr_pct,adx,regime,bollinger_*}`, `divergence`, `round_number`, momentum`.stochastic_*`, volume`.obv_rising`; all via `_num()`/`_last()` so build_facts tolerates a frame lacking toolkit columns (hand-built fixtures) and stays JSON-safe. WHY facts-first (advisor-vetted): (1) with 4 active categories + require_categories=2, promoting a 5th voteable category makes triggering EASIER → less selective, eroding the Phase-17 selectivity win; (2) Phase-17 showed breadth didn't help, so don't promote on faith; (3) an ungated Bollinger mean-reversion vote is wrong in a trend (needs ADX regime-gating first). Promotions are a later MEASURED step (by ablation; RSI-divergence is the best a-priori candidate as it carries genuinely new info; Bollinger last, ADX-gated). Backtest is unchanged from Phase 17 (no votes touched) — used as a regression check. ATR now exists for Phase 19's ATR-relative calibration.

Phase 17 conventions: the confluence engine is now CATEGORY-AWARE with a 0–1 confidence (the tally is gone). `SIGNAL_CATEGORY` maps each detector to an INDEPENDENT category — trend / momentum{rsi,macd} / structure{candlestick,support_resistance,fibonacci} / volume (volatility empty until Phase 18). `_category_net` collapses each category's signals to ONE majority vote — this kills the S/R+fib double-count the plan named (rsi+macd likewise count once). `evaluate_confluence(signals, cfg, *, require_categories=None)` → `ConfluenceResult{bias, triggered, confidence, agreeing_categories, categories, signals, mtf_*}`: the heavier-WEIGHT side wins (weight tie → neutral), `confidence = winning_weight / active_category_weight` (breadth×weight, genuine 0–1), `triggered = agreeing_categories >= require_categories AND not MTF-conflict`. CONTRACT: confidence is PRE-MTF out of `evaluate_confluence`, FINAL out of `resolve_mtf` (grades ×1.1 aligned / ×0.5 conflict, capped 1.0; conflict still hard-vetoes — the validated Phase-16 gate kept, not replaced). `analyze_confluence` now ALSO runs `resolve_mtf` (lazy import to avoid a cycle) so `show_confluence.py` and any one-call consumer see the same final verdict as facts/backtest. `contributing`/`reasons` derive from the WINNING CATEGORIES (category-consistent — a lone bullish candle in a bearish-netting structure category is not listed). Removed `ConfluenceResult.agreeing`; config `min_agreeing_signals` kept VESTIGIAL (superseded), new `require_categories`(2) + `category_weights` (NOT tuned to the backtest). Backtest: `SetupOutcome` carries `confidence`+`agreeing_categories`; report buckets by breadth (`2 cats` vs `3+ cats`, N shown). CLI `--require-categories`. **Findings (BTC/USDT 1h, h=24, step=4): overall win-rate 51.3% — best of any phase (vs Phase-16 49.3%), bullish 54.7% — but the win is the category COLLAPSE making the engine SELECTIVE (429 setups vs 691), NOT the weights: weighted 51.3% ≈ uniform 50.9% (weights earned ~nothing, kept as neutral priors). Breadth thesis did NOT hold: 3+ cats 47.9% (n=48, thin) < 2 cats 51.7% — reported honestly, weights NOT tuned to fix it.** See [[phase17-confidence-findings]].

Phase 16 conventions: `structure/mtf.py` gates the base-timeframe setup against the higher timeframes. It RESAMPLES the base candles up to each higher TF (`resample_ohlcv`; OHLC=first/max/min/last, volume=sum) rather than fetching a separate series — so `resample_ohlcv(df[:i+1])` is look-ahead-safe in the backtest. `higher_timeframe_trend` runs `add_features`+`find_swings`+`classify_trend` on the resampled frame, but ONLY once it has `>= indicators.slow_ma` (50) resampled bars — below that it returns None (dormant), which both matches "the 1d gate needs ~50 daily bars (~1200 hourly)" AND dodges a latent `add_features` crash (pandas-ta's `ta.macd` returns None under 26 rows → `macd[...]` raises; add_features is still fragile on <26-row frames, just now unreachable). `build_mtf_context` takes the trend of each TF strictly above the base and sets `aggregate_bias` = the HIGHEST directional TF (1d before 4h). The mechanism is a GATE, not a vote (tally era): `resolve_mtf` downgrades a `conflict` setup (`triggered→False`, `mtf_downgraded=True`, bias preserved), passes aligned/neutral, and returns `mtf_alignment=None` when disabled or no higher TF applies (distinct from `"neutral"`). `ConfluenceResult` gained `mtf_alignment`/`mtf_trends`/`mtf_downgraded` (defaults keep it backward-compatible; confluence does NOT import mtf — one-way dep). Wired into `facts.py` (after `evaluate_confluence`; adds a HIGHER TIMEFRAMES render + `mtf_*` keys) and backtest `signal_at` (gate measured; report shows "vetoed N"). Config: `mtf.{enabled, context_from=[4h,1d]}` via `default_factory`. The look-ahead guard test uses an ACTIVE (directional) 4h context and asserts the aggregate is directional BEFORE asserting invariance (the Phase-14 lesson: a neutral-context guard passes for the wrong reason). Phase 17 replaces this binary gate with a graded confidence multiplier. **Backtest (BTC/USDT 1h, h=24, step=4, min_agreeing=2), enabled vs disabled at identical params (isolates the gate): vetoed 135 setups; overall 48.3%→49.3%, bullish 50.8%→53.1% (+2.3pp, the clear win), bearish 45.2%→44.6% (slightly worse; net-bullish sample). Helps modestly, mainly long side; disabled path is byte-identical to pre-MTF by construction.**

Phase 15 conventions: volume is now a 7th confluence vote. `features.py` adds `COL_VOLUME_MA` (SMA of volume, period `indicators.volume_ma=20`) — the column is ALWAYS present to honour the `INDICATOR_COLUMNS` contract, but is all-NaN when a frame carries no `volume` column (some synthetic/forex frames), and the detector treats that as "no confirmation" rather than crashing. `signal_from_volume(featured_df, cfg)` CONFIRMS the last candle's direction (up candle→bullish, down→bearish) only when the bar traded `>= confluence.volume_confirm_factor` (1.2) × its volume MA; below that bar, or with volume missing/NaN, it votes NEUTRAL — a move on thin volume adds no weight. Volume has no direction of its own, so it's confirmation, not an independent directional signal. The vote is wired into BOTH `gather_signals` and `build_facts` (the Phase-7 consistency guarantee — they must stay in lock-step; `test_facts.py` pins `len(signals)==7`). `facts["volume"]` is `{last, average, ratio, confirmed}` or None, rendered as a VOLUME section. Phase 17 will REPLACE this binary vote with a proper strength multiplier once confluence carries a 0–1 confidence. **Backtest delta vs the Phase-14 baseline (BTC/USDT 1h, h=24, min_agreeing=2, step=3): overall 49.7%→50.4%, median −0.01%→+0.02%, bearish 45.8%→47.2% — not worse, marginally better, but WITHIN NOISE (overlapping windows aren't independent).**

Phase 14 conventions: `backtest/evaluate.py` is a FORWARD-RETURN evaluator, not a trade simulator — `evaluate(df, cfg, *, horizon=24, warmup=None, min_agreeing=None, step=1)` walks history, runs the confluence engine at each bar, and on a triggered bullish/bearish setup measures `close[i]→close[i+horizon]` aligned to the bias (positive = moved the way the setup implied), aggregating per-bias + overall as `Stats(n, win_rate, avg, median)`. LOOK-AHEAD SAFETY is the whole point: `signal_at(df, i, cfg, *, featured=None, min_agreeing=None)` computes the verdict from `df[:i+1]` ONLY — swings are RECOMPUTED on the slice (the confirmed-interior filter in `find_swings` means the last `sensitivity` bars can't be pivots, so nothing after `i` leaks), and `gather_signals` reads just `.iloc[-1]` + those swings. `add_features` is CAUSAL (SMA/RSI/MACD past-only; patterns use only current + `shift(1)`), so `evaluate` computes features ONCE on the full frame and slices for speed; `test_backtest.py`'s guard proves this end-to-end by multiplying bars after `i` by 10 (finite garbage, NOT NaN — NaN can't perturb a past EWM and would pass for the wrong reason) and asserting the verdict at `i` is unchanged. Loop stop is `range(warmup, len(df)-horizon)` (last valid `i` is `len-1-horizon`). Report ALWAYS prints n next to each win-rate and carries a CAVEAT that overlapping forward windows (small `step`) make N non-independent. **BASELINE (BTC/USDT 1h, horizon=24, min_agreeing=2): overall win-rate ~49.7% (n=1053) — essentially coin-flip; bullish 53.2% / bearish 45.8%. Phases 15–18 must beat THIS.** See [[phase14-backtest-baseline]].

Phase 13 conventions: the data layer is now provider-abstracted. `data/base.py` holds the `DataProvider` ABC (`fetch(symbol, timeframe, limit)`) + asset-class constants (`CRYPTO`/`FOREX`) and NOTHING else, so nothing imports back into it (no cycle). `crypto_ccxt.py`'s `CryptoProvider` WRAPS `exchange.fetch_ohlcv` (not a rename — `exchange.py`/`download_data.py`/`test_exchange.py` stay intact). `forex_api.py`'s `ForexProvider.fetch` raises `NotImplementedError` (a deliberate Phase-26 slot — ccxt is crypto-only). The app-facing facade is `registry.get_candles(symbol, timeframe, cfg, *, refresh=False, data_dir=...)`: picks a provider by `asset_class_for(symbol)` (registered pairs authoritative; unknown symbol → crypto), is CACHE-FIRST (reuse cached CSV → else `provider.fetch` + `save_candles`), then runs the quality gate; it has NO TTL by design (freshness is Phase 24). `advise()` now calls `get_candles` instead of `load_candles` — cache-first makes the Phase-12 regression byte-identical (verified: facade vs direct-cache facts are `==`). `quality.py`: `check_candles` is READ-ONLY → `QualityReport` whose `.ok` covers ONLY structural corruption (dup timestamps, NaN OHLC, non-monotonic, high<low); gaps/outliers are ADVISORY (reported, never flip `.ok`, never raise) because real markets have them — verified `.ok is True` on the real 4320-bar cached CSV. Gap interval is derived from `median(index.diff())`, not by parsing the timeframe string. `clean_candles` mirrors `normalize_ohlcv` (dropna→de-dup keep-last→sort) and KEEPS volume (Phase 15 needs it). `data/__init__.py` stays empty on purpose (so importing `src.data.*` submodules doesn't drag in ccxt); import the facade from `src.data.registry`.

Phase 12 conventions: `service/analyze.py`'s `advise(symbol, timeframe, cfg, *, df=None, client=None) -> AnalysisResult` is the JSON seam every front door (CLI, API, UI) sits on. `symbol`/`timeframe` are PER-REQUEST args, not read from `config.yaml`: `advise` builds a request-scoped config via `_request_config` (`cfg.market.model_copy(...)`, never mutating the caller's cfg) so `build_facts`, the cache filename, and the chart slug all reflect the requested pair. Compute-once is preserved exactly as Phase 7/8 — swings computed once, driving both the facts and the detector objects (`swings/levels/trendlines/fib`) carried on the result. `AnalysisResult.to_payload()` returns the JSON-serializable dict (`{**facts, "explanation": ...}`); the facts dict is JSON-safe by construction (fib/pattern prices are `float()`-cast at their source). Explanation is key-gated (`explain(..., client=...)`; `RuntimeError` on a missing key → `explanation=None`); `client` is injectable so tests cover the Layer 2 path keyless. `scripts/analyze.py` is now a THIN wrapper: `run_analysis(df, cfg, outputs_dir)` (same signature — `test_chart.py` unchanged) calls `advise()` then `_write_outputs` renders the PNG + markdown from the result's carried objects. `to_payload`'s flat shape is provisional — Phase 24's `serialize.py` owns the real API contract.

Phase 3 conventions: swings via `scipy.argrelextrema` on `high`/`low` (not close), filtered to fully-confirmed interior positions `[sensitivity, n-1-sensitivity]` — argrelextrema's default `mode='clip'` otherwise flags spurious unconfirmed pivots on the newest bars. `find_swings` returns a tidy frame keyed by integer `bar` (not timestamp — an outside bar can be both high and low). Structure detectors build on `bar`.

Phase 4 conventions: S/R `tolerance_pct` is a PERCENT (÷100 to a fraction — a 0.4%-merge/0.6%-no-merge test guards this). Detectors return ALL levels; capping/proximity selection lives in the display script, not the detector. Trendlines are least-squares fits (`numpy.polyfit`) through the last 3 swings with an `r2` quality gate; draw them from the first anchor bar forward (never extend a short local fit backward across the window). `classify_trend` reads `COL_SMA_SLOW` and requires the featured frame to be row-aligned with the swings' candles; `sideways` is the neutral fallback, and an unconfirmed HH/HL (MA not rising) downgrades to sideways.

Phase 10 conventions (OPTIONAL, secondary path): `advisor/vision.py`'s `explain_chart_image(image_path, cfg, client=None, question=None)` sends a chart SCREENSHOT to Claude's vision. Deliberately secondary — pixels are a worse data source than the computed numbers, and `VISION_SYSTEM_PROMPT` says so (read what's visible, be explicit about what can't be measured, defer to the data pipeline for precision, no advice). Same conventions as `explain.py`: config-driven model, injectable client, cached system prompt, `type=="text"` extraction. New piece: base64 image content block via `build_image_messages`; `_media_type` maps extension→MIME and raises on unsupported. `scripts/analyze_image.py <path> [question]`. **Phase 10 VERIFIED live 2026-09-06 against `outputs/analysis_BTC-USDT_1h.png`: correctly read the S/R zones, resistance trendline, and the bullish marker as a pullback-to-support, appropriately hedged.** Offline tests cover plumbing only (media type, base64 packing, model, extraction).

Phase 9 conventions (OPTIONAL, fuzziest detector): `patterns/chart_patterns.py` detects double top/bottom, head & shoulders / inverse, and triangles (asc/desc/sym) via geometry on the recent tail of swings (`find_chart_patterns`, default lookback 7). Anti-over-call is the whole game: TWO tolerances — peaks "equal" within `patterns.price_tolerance_pct` AND the trough/peak between them deep enough (`patterns.min_trough_pct`) to be a real reversal; H&S head must clear shoulders by > tolerance. Triangle flatness is NORMALIZED by price (`slope*span/mean_price*100` vs `patterns.flat_slope_pct`) — raw price-per-bar slope is meaningless across symbols. Walkers read highs/lows independently and tolerate non-alternating swings. Patterns CAN co-occur (an ascending triangle's near-equal highs also read as a double top) — that's by design; tests assert the expected label is PRESENT, not exclusive. New `PatternsConfig` (default via `Field(default_factory=...)` so `config.yaml` can omit the block without breaking `load_config`) + a `patterns:` yaml block. Wired into `facts.py` as an additive `chart_patterns` field (NOT a confluence vote — preserves Phase 6/7 semantics), rendered in `facts_to_prompt` LABELED best-effort/approximate so the model calibrates confidence; explain.py's system prompt says the same. Phase 9 is FULLY verified offline (textbook fixtures are the "done when"); also runs clean/no-false-positives on real cached swings via `show_patterns.py`.

Phase 8 conventions: `viz/chart.py`'s `render_chart` is a PURE renderer — it takes already-computed detector objects (swings/levels/trendlines/fib) and never recomputes. `scripts/analyze.py` computes `swings` ONCE and derives levels/trendlines/fib from it with config params, so the drawn geometry is byte-identical to what `build_facts` computed (same pure functions, same inputs) — the nearest-support line on the chart is the number the explanation quotes. `render_chart` is the display layer, so it owns selection (nearest-N-per-side S/R via `_top_levels`, r²-gated anchor-forward trendlines, fib band clamped to `start_bar`) — mirrors the show_* scripts. Sets `matplotlib.use("Agg")` at import (headless PNG). Pass mplfinance kwargs (`addplot`/`hlines`/`alines`) ONLY when non-empty — `addplot=None` raises. The confluence marker reflects CURRENT state only (engine votes on `.iloc[-1]`, no per-bar history): mark the last candle colored by bias, only when `triggered` — NOT a rolling backtest. `analyze.run_analysis(df, cfg, outputs_dir)` is the testable core returning saved paths; it saves the chart UNCONDITIONALLY and only the explanation is key-gated (falls back to facts-only markdown on `RuntimeError` from a missing key). Two artifacts in `outputs/` (gitignored): `analysis_<slug>_<tf>.png` + `.md` — "side by side", not a composite. **Full "done when" VERIFIED live 2026-09-06: `analyze.py` wrote the annotated PNG plus a markdown carrying the real Claude explanation, side by side in `outputs/`.**

Phase 7 conventions: `facts.py` is the Layer1→Layer2 seam. `build_facts` computes each detector (trend/S/R/fib) EXACTLY ONCE with config params and feeds the same objects into both the confluence evaluation AND the facts dict — this is the consistency guarantee that makes "Layer 2 never contradicts Layer 1" provable (a second differently-parameterized pass would let display and vote disagree by luck; a test pins it). It does NOT call `analyze_confluence` (which recomputes); it wires the per-signal functions itself over the shared objects. `explain.py` is Layer 2: `client.messages.create` with the CONFIG-DRIVEN model (`cfg.advisor.model` — the user's `config.yaml` choice IS them naming a model; do not hardcode opus), no `thinking` param (keeps it model-agnostic across whatever the config names), `cache_control` on the stable `SYSTEM_PROMPT` (a no-op below Sonnet's 2048-token cache minimum but correct placement). `explain(facts_text, cfg, client=None)` takes an injectable client so tests run without a key; extracts only `type=="text"` blocks. System prompt tells the model the facts are complementary (distance-ranked "nearest resistance" vs proximity-gated S/R vote answer different questions) so it doesn't manufacture contradictions. **Phase 7 "done when" VERIFIED live 2026-09-06 with `claude-sonnet-4-6`: `show_explanation.py` produced a clear, accurate, teaching explanation matching every computed fact with no Layer 1 contradiction (it framed the bearish MACD as disagreeing rather than manufacturing a conflict). Key lives in gitignored `.env`; `.env.example` holds only the placeholder.**

Phase 6 conventions: confluence is still Layer 1 — it *tallies*, it doesn't reason. Split in two: `evaluate_confluence(signals, min_agreeing)` is pure (hand it a `Signal` list, assert the flag — this is the "done when" check); `gather_signals(featured_df, swings, cfg)` wires the real detectors. Vote rule: the side with STRICTLY more directional votes wins, and fires only if it reaches `min_agreeing_signals`; a tie (equal bull/bear) never fires (bias `neutral`). Neutral votes are recorded but never count toward the threshold; NaN indicator values on short frames vote NEUTRAL, not a NaN comparison. `ConfluenceResult.reasons`/`.contributing` return the WINNING side's signals only. Six votes: trend, rsi, macd, candlestick, support_resistance, fibonacci (no trendline vote — it overlaps S/R proximity). Proximity uses a NEW config field `confluence.proximity_pct` ("is price at a level now"), deliberately distinct from `structure.sr_cluster_tolerance_pct` (swing-merge). Detectors are recomputed here; Phase 7's `facts.py` will recompute too — fine to refactor later.

Phase 5 conventions: `fib_retracement` auto-picks the latest leg (last swing + most recent opposite swing before it). Invariant: `levels[0.0]` is the most-recent swing's price (impulse end), `levels[1.0]` the older anchor's — this pins the up/down branch (the 0.5 midpoint can't). Returns `None` on a degenerate `high <= low` leg (from non-alternating swings). Returns `FibRetracement` (dataclass), not columns on the frame.

Env note: developed on Python 3.14; all deps (incl. the `pandas-ta-classic` git build) install cleanly. `fetch_ohlcv` pages with a `since` cursor because exchanges cap ~1000 candles/call. `normalize_ohlcv` is kept network-free and unit-tested; the real fetch is a `@pytest.mark.network` test (skip offline with `pytest -m "not network"`).

Phase 2 conventions: TA-Lib is **not** installed, so candlestick patterns are hand-rolled boolean columns in `features.py` (not `cdl_pattern`). Indicator/pattern column names are exported as `COL_*` constants + `INDICATOR_COLUMNS`/`PATTERN_COLUMNS` — import those downstream, don't hardcode strings. `add_features` keeps warmup NaNs (never drops rows). MAs are SMA; RSI/MACD use pandas-ta defaults (match TradingView against the last *closed* bar).

## What this project is

A **visual trading advisor** (Python) that analyzes market candles, computes chart structure (trend, support/resistance, trendlines, Fibonacci, patterns), flags higher-confidence setups when signals agree, and explains its reasoning in plain trading language. It **does not place trades and does not predict the future** — its value is clarity and education.

## Core architecture: two layers (do not blur them)

The single most important design rule: **separate the facts from the explanation.**

- **Layer 1 — the eyes (deterministic).** Pure math/rules on price data: indicators, candlestick patterns, swing points, S/R, trendlines, trend direction, Fibonacci. Outputs plain structured facts (numbers + labels). Lives in `src/indicators/`, `src/structure/`, `src/patterns/`, `src/signals/`.
- **Layer 2 — the brain/voice.** A Claude model (`src/advisor/`) receives Layer 1's structured facts and reasons about them in trading language. It reasons over **computed facts, never raw pixels**.

**Layer 2 must never override or contradict Layer 1.** Layer 1 facts are authoritative; if the explanation disagrees with the computed facts, that is a bug in how facts are assembled/fed, not a value judgment. `advisor/facts.py` assembles facts into a structured summary; `advisor/explain.py` sends them to Claude.

**Swing-point detection (`structure/swings.py`) is the foundation.** Support/resistance, trendlines, trend direction, and named patterns are all built on top of swing points. Prioritize getting it correct and well-tested before anything downstream.

## Data flow

`src/advisor_run.py` orchestrates the pipeline:
`data (ccxt) → indicators + structure detectors → confluence → facts → Claude explanation → annotated chart`

The confluence engine (`signals/confluence.py`) collects a bullish/bearish/neutral vote plus a short reason from each detector, and flags a setup only when a configurable number agree (`config.yaml: confluence.min_agreeing_signals`), recording *why*.

## Key libraries & conventions

- Use **`pandas-ta-classic`** (the fork), **not** the original `pandas-ta` — install via `pip install "git+https://github.com/xgboosted/pandas-ta-classic"`.
- Market data via **`ccxt`**, public OHLCV only — this app never needs trade or withdrawal permissions; read-only at most.
- `pandas` DataFrame is the core data structure; indicators are added as columns.
- `scipy` for peak/valley (swing) detection, `numpy` for line fitting/clustering, `mplfinance` for annotated charts.
- Config: `pydantic`-validated `config.yaml` (symbol, timeframe, detector sensitivity, model) + `.env` for `ANTHROPIC_API_KEY` only. `.env` is never committed; commit `.env.example` with the key name only.
- Sensitivity/thresholds (swing sensitivity, S/R cluster tolerance, RSI periods, min agreeing signals, model name) are config-driven — do not hardcode them in detectors.

## Environment & commands

Python 3.11+. Intended setup (per PLAN.md):

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt          # once requirements.txt exists

# run the advisor: prints explanation + saves annotated chart to outputs/
python scripts/analyze.py

# one-off history download into data/ (gitignored cache)
python scripts/download_data.py

# tests (detectors and confluence are the priority to test)
pytest
pytest tests/test_swings.py              # run a single test file
pytest tests/test_swings.py::test_name   # run a single test
```

`data/` (cached candles) and `outputs/` (charts + explanations) are gitignored working directories.

## Build order (phases in PLAN.md)

Build the deterministic tier and structure detection *before* the reasoning layer, so Claude always has correct facts. Order: 0 skeleton → 1 data → 2 indicators/candlestick patterns → 3 **swings** → 4 structure (S/R, trendlines, trend) → 5 Fibonacci → 6 confluence → 7 Claude explanation → 8 visualization. Phases 9 (named chart patterns) and 10 (chart-image/vision analysis) are optional and added last. Each phase has a "done when" check in `PLAN.md`; verify it before moving on.
